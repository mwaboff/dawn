I need to implement three major frontend features for my Angular app. Complete plan documents exist at:

- `docs/plans/2026-04-02-master-plan.md` — overall orchestration
- `docs/plans/2026-04-02-ws-a-foundation.md` — foundation (models, services, auth)
- `docs/plans/2026-04-02-ws-b-campaigns.md` — campaign feature
- `docs/plans/2026-04-02-ws-c-public-profiles.md` — public player profiles
- `docs/plans/2026-04-02-ws-d-nav-routing-join.md` — navbar, routing, join flow

Read ALL five plan documents first. Then execute them as follows:

1. **Workstream A (Foundation)** — implement directly on the current branch. This creates shared models, services, auth updates, and placeholder routes that the other workstreams depend on. Commit when done.

2. **Workstreams B, C, D (Parallel)** — after A is committed, launch three agents in parallel worktrees (`isolation: "worktree"`) to implement B, C, and D simultaneously. Each agent should read its respective plan document and implement everything described, including tests.

3. **Integration** — after all three worktrees complete, merge them back, resolve any conflicts (likely in `app.routes.ts` and navbar), and do a final validation.

**Validation requirement (every workstream):** `npm run test:run && npm run lint && npm run build` must all pass.

Use the `core-api-blueprint` skill if you need to look up any backend API details. The API reference files are at `.agents/skills/core-api-blueprint/references/`.

Follow all conventions in CLAUDE.md and AGENTS.md. Use OnPush, signals, standalone components, vanilla CSS with custom properties, the warm tavern aesthetic (dark browns, gold #d4a056, parchment tones, Cinzel/Lora fonts).
